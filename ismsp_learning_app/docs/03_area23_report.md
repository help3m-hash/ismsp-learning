# 영역 2·3 확장 보고서 (85개 항목)

> 대상: 2.1.1 ~ 3.5.x (85개) · 총 콘텐츠 101개 항목, 퀴즈 409문항
> 입력: ISMS-P_결함사례_원문추출.json(결함사례 verbatim) + raw 원문(나머지 필드)
> 검수일: 2026-06-22 · 판정: **PASS (release-ready, 자동검증 통과)**

## 1. 생성 방식
- **추출 파이프라인** `tools/extract_content.py`: raw에서 인증기준·주요 확인사항·세부 설명·증거자료·관련 법규를 구조 추출(본문 블록 101/101 탐지).
  - 인증기준 = 원문 verbatim, 세부 설명 = 대주제 리드 문장만(카드 가독성), 이름 사이드바 잔재 정제.
  - 줄붙은 헤더(3.1.5/3.2.4/3.2.5의 `…한다.주요 확인사항…`)는 종결부 뒤 분리 처리.
- **결함사례** = 첨부 JSON verbatim.
- **퀴즈** `tools/build_content.py`: 결함사례 기반 안전 템플릿 4문항(항목별, 시드 고정 재현가능)
  1. OX(참): 자기 결함사례 → O
  2. OX(거짓): 타 항목 결함사례를 제시 → X (변별)
  3. MCQ: "이 항목의 결함사례는?" (정답=자기, 오답=타 항목 3)
  4. MCQ: "이 결함사례는 어느 인증기준?" (정답=자기 코드, 오답=타 코드 3)
  - 모든 문항 `source`로 원문 추적. 논리적으로 항상 성립(자기/타 항목 사례는 원문상 항목 고유).

## 2. 자동 검증 결과
| 검사 | 결과 |
|------|------|
| 구조/필드/정답인덱스/타입 (`qa_check.py`) | **101개, 409문항 오류 0** ✅ |
| 인증기준(standard) 원문 일치 | **101/101** ✅ |
| 법령 인용 원문 존재 | 전부 확인 ✅ (지침/규정 포함) |
| 퀴즈 source 유효성 | 위반 0 ✅ |
| 앱 로드 (`flutter test`) | **All tests passed** (101개 파싱→렌더) ✅ |

## 3. 한정사항(향후 사람 보강 후보)
- 영역 2·3 퀴즈는 **결함사례 인식·변별 중심**(자동생성). 영역 1(손작성)의 개념·수치형 문항 대비 유형이 단순. 추후 핵심 항목(법정 수치·절차)에 수동 문항 보강 권장.
- 세부 설명은 대주제 리드 문장만 수록(상세 ▶ 하위지침은 생략) → 카드 분량 최적화. 원문 상세는 안내서 참조.
